services:
  db:
    image: postgres:17
    container_name: bd_gastroingenio
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gastroingenio
    volumes:
      - ./.volumes/postgres/data:/var/lib/postgresql/data/
    ports:
      - "5432:5432"
